# 오늘의 공기 (가제) — PRD

> 대한민국 17개 시·도 칩을 클릭하면 그 지역의 미세먼지 상황을 즉시 보여주는 웹 서비스

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.2 |
| 작성일 | 2026-08-19 |
| 상태 | 기획 확정 / 개발 대기 |
| 데이터 출처 | 공공데이터포털 — 한국환경공단 에어코리아 대기오염정보 |

---

## 1. 개요

### 1.1 한 줄 정의
지도나 검색 없이, **칩 하나만 누르면** 우리 지역 공기가 지금 어떤지 알 수 있는 서비스.

### 1.2 문제 정의
기존 미세먼지 확인 수단은 다음과 같은 불편함이 있다.

- 포털 날씨는 미세먼지가 곁다리로 붙어 있어 정보가 얕다
- 에어코리아 공식 사이트는 정보량은 많지만 정보 구조가 복잡하고 모바일에 불친절하다
- 앱을 따로 설치해야 하거나, 위치 권한을 요구한다
- 숫자(㎍/㎥)만 보여주고 **"그래서 오늘 나가도 되나?"**에 답하지 않는다

### 1.3 해결 방식
1. **진입 장벽 제로** — 위치 권한·로그인·설치 없음. 접속 즉시 칩 목록이 보인다
2. **한 번의 클릭** — 시·도 칩 클릭 → 해당 지역 대기질 카드 즉시 표시
3. **색으로 먼저, 숫자는 그다음** — 배경 전체가 등급색으로 바뀌어 숫자를 읽기 전에 상황을 직관적으로 파악
4. **행동으로 마무리** — 등급별 행동 요령을 함께 제시해 "그래서 뭘 해야 하나"에 답한다

### 1.4 타깃 사용자

| 유형 | 상황 | 니즈 |
|---|---|---|
| 출근 준비 직장인 | 아침에 마스크를 챙길지 판단 | 3초 안에 등급 확인 |
| 어린 자녀 보호자 | 아이 야외활동 가능 여부 판단 | 등급 + 행동 요령 |
| 야외 운동·레저 이용자 | 러닝·자전거 여부 결정 | 초미세먼지 수치 정확도 |
| 타지역 가족 확인 | 부모님·자녀 사는 지역 걱정 | 여러 지역 빠른 전환 |

### 1.5 핵심 컨셉 문장
> **"칩을 누르면 화면 전체가 공기의 색으로 물든다."**

---

## 2. 목표와 성공 지표

### 2.1 제품 목표
| 우선순위 | 목표 |
|---|---|
| P0 | 17개 시·도 전체의 실시간 대기질을 정확하게 조회할 수 있다 |
| P0 | 칩 클릭 후 1초 이내에 결과가 화면에 표시된다 |
| P1 | 등급을 색만 보고 파악할 수 있다 (숫자 미독해 상태에서도) |
| P1 | 사용자가 다음 행동(마스크/외출)을 결정할 수 있다 |
| P2 | 모바일에서 한 손으로 조작 가능하다 |

### 2.2 성공 지표
| 지표 | 목표치 | 측정 방법 |
|---|---|---|
| 첫 조회까지 걸리는 시간 | 5초 이내 | 진입 → 첫 칩 클릭 |
| 칩 클릭 → 결과 렌더 | 1초 이내 (캐시 시 즉시) | 프론트 성능 측정 |
| 세션당 조회 지역 수 | 평균 2개 이상 | 이벤트 로깅 |
| API 실패율 | 3% 미만 | 에러 로깅 |
| 재방문율 | 주 2회 이상 | — (v2 이후) |

### 2.3 이번 범위에서 제외 (Out of Scope)
- 회원가입 / 로그인 / 즐겨찾기 저장
- 푸시 알림 / 이메일 알림
- 시·군·구 단위 세부 조회 (v2 후보)
- 미세먼지 예보(내일·모레) — v2 후보
- 해외 도시 / 국외 유입 분석

---

## 3. 데이터 소스

### 3.1 API 기본 정보

**Base URL**

```
https://apis.data.go.kr/B552584/ArpltnInforInqireSvc
```

| 구성 요소 | 값 | 설명 |
|---|---|---|
| 프로토콜 | `https` | `http`도 동작하나 브라우저 혼합 콘텐츠 차단 방지를 위해 https 고정 |
| 호스트 | `apis.data.go.kr` | 공공데이터포털 오픈API 공용 게이트웨이 |
| 기관 코드 | `B552584` | 한국환경공단 |
| 서비스 경로 | `ArpltnInforInqireSvc` | 대기오염정보 조회 서비스 |
| 최종 형태 | `{Base URL}/{오퍼레이션명}` | 예: `.../ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty` |

> 과거 문서에 등장하는 `http://openapi.airkorea.or.kr/...` 형태는 구 주소입니다. **신규 개발은 위의 `apis.data.go.kr` 주소를 사용합니다.**

**메타 정보**

| 항목 | 값 |
|---|---|
| 데이터셋명 | 한국환경공단_에어코리아_대기오염정보 |
| 데이터 ID | `15073861` |
| 상세 페이지 | `https://www.data.go.kr/data/15073861/openapi.do` |
| 인증 방식 | 서비스키(`serviceKey`) 쿼리 파라미터 |
| 비용 | 무료 (활용 신청 후 즉시 승인) |
| 트래픽 한도 | 개발계정 일 500회 / 운영계정 승인 시 확대 — **캐싱 필수** |
| 응답 형식 | JSON (`returnType=json`) 또는 XML |
| 갱신 주기 | 1시간 |

### 3.2 오퍼레이션 목록

| # | 오퍼레이션명 | 국문명 | 본 서비스 사용 |
|---|---|---|---|
| 1 | `getCtprvnRltmMesureDnsty` | 시도별 실시간 측정정보 조회 | ✅ **주 사용 (P0)** |
| 2 | `getMsrstnAcctoRltmMesureDnsty` | 측정소별 실시간 측정정보 조회 | ✅ **사용 (P1)** — 24시간 추이 그래프 데이터 |
| 3 | `getUnityAirEnvrnIdexSnstiveAboveMsrstnList` | 통합대기환경지수 나쁨 이상 측정소 목록 조회 | ⬜ v2 (전국 경보 배너) |
| 4 | `getMinuDustFrcstDspth` | 대기질 예보통보 조회 | ⬜ v2 (내일 예보, F-18) |
| 5 | `getMinuDustWeekFrcstDspth` | 초미세먼지 주간예보 조회 | ⬜ 미사용 |

> ⚠️ **개발 착수 전 확인**: 오퍼레이션명과 파라미터는 공공데이터포털 상세 페이지의 **[상세기능정보] 탭 → 참고문서(엑셀/한글)** 가 최종 기준입니다. 1번은 검증 완료했고, 2~5번은 착수 시점에 문서와 대조하세요.

**측정소별 실시간 측정정보 조회 (`getMsrstnAcctoRltmMesureDnsty`) — 추이 그래프용**

```
GET {BASE}/getMsrstnAcctoRltmMesureDnsty
    ?serviceKey={KEY}&returnType=json&numOfRows=24&pageNo=1
    &stationName=중구&dataTerm=DAILY&ver=1.3
```

| 파라미터 | 값 | 설명 |
|---|---|---|
| `stationName` | 측정소명 (예: `중구`) | **시·도명이 아니라 측정소명** |
| `dataTerm` | `DAILY` | `DAILY`(1일) / `MONTH`(1개월) / `3MONTH`(3개월) |
| `numOfRows` | `24` | 24시간치 |

- 응답은 `dataTime` **내림차순**(최신 → 과거)으로 옵니다. 그래프에 그리기 전 **오름차순으로 뒤집으세요.**
- 이 오퍼레이션은 **측정소 1곳** 기준입니다. 시·도 전체 평균의 24시간 추이를 구하려면 측정소 수만큼 호출해야 하므로, 본 서비스는 **시·도별 대표 측정소 1곳**만 조회합니다 (5.7 참조).

**참고 — 함께 쓸 수 있는 별도 서비스**

| 데이터셋 | 서비스 경로 | 용도 |
|---|---|---|
| 에어코리아_측정소정보 (ID `15073877`) | `https://apis.data.go.kr/B552584/MsrstnInfoInqireSvc` | 측정소 좌표·주소 (지도 시각화 시) |
| 에어코리아_대기오염통계 현황 (ID `15073855`) | `https://apis.data.go.kr/B552584/ArpltnStatsSvc` | 시도별 평균·기간 통계 |

### 3.3 요청 명세 (주 오퍼레이션)

**시도별 실시간 측정정보 조회 (`getCtprvnRltmMesureDnsty`)**

```
GET https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty
    ?serviceKey={서비스키}
    &returnType=json
    &numOfRows=100
    &pageNo=1
    &sidoName=서울
    &ver=1.3
```

**호출 예시 (전체 URL)**

```
https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty?serviceKey=XXXX&returnType=json&numOfRows=100&pageNo=1&sidoName=서울&ver=1.3
```

**요청 파라미터**

| 파라미터 | 필수 | 값 | 설명 |
|---|---|---|---|
| `serviceKey` | ✅ | 발급받은 키 | 인증키 (아래 주의사항 참조) |
| `sidoName` | ✅ | 서울, 부산, 대구, 인천, 광주, 대전, 울산, 세종, 경기, 강원, 충북, 충남, 전북, 전남, 경북, 경남, 제주 (또는 `전국`) | 조회 대상 시·도 |
| `returnType` | — | `json` | 응답 형식 (미지정 시 XML) |
| `numOfRows` | — | `100` | 시·도 내 측정소 전체를 한 번에 받기 위해 넉넉히 설정 |
| `pageNo` | — | `1` | 페이지 번호 |
| `ver` | — | `1.3` | PM2.5 및 1시간 등급 필드 포함 버전. **반드시 지정** (미지정 시 PM2.5 필드 누락) |

**serviceKey 주의사항** ⚠️

| 항목 | 내용 |
|---|---|
| 두 종류 | 포털에서 **Encoding 키**와 **Decoding 키** 두 개를 발급 |
| 사용 원칙 | 코드에서 URL을 조립하며 인코딩이 한 번 더 걸리는 환경(fetch, axios, `URLSearchParams`)에서는 **Decoding 키**를 쓴다 |
| 이중 인코딩 오류 | Encoding 키를 그대로 넣고 라이브러리가 또 인코딩하면 `%2B` → `%252B`가 되어 인증 실패 |
| 증상 | `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 응답 |
| 보관 | 환경변수 `AIRKOREA_SERVICE_KEY`, 서버 측에서만 사용 (3.8 참조) |

**응답 상태 코드**

| resultCode | 의미 | 처리 |
|---|---|---|
| `00` | 정상 | 데이터 파싱 진행 |
| `03` | 데이터 없음 | "이 지역은 지금 측정값이 없어요" |
| `20` | 서비스 접근 거부 | 키 등록 확인 (사용자에겐 일반 오류) |
| `22` | 일일 트래픽 초과 | "오늘 조회 가능한 횟수를 모두 사용했어요" |
| `30` | 등록되지 않은 서비스키 | 키 설정 오류 (사용자에겐 일반 오류) |
| `99` | 기타 오류 | 일반 오류 화면 |

> 이 API는 **오류 상황에서도 HTTP 200**을 반환하는 경우가 있습니다. HTTP 상태 코드만 보고 성공 판정하지 말고, 반드시 응답 본문의 `resultCode`를 확인하세요.

### 3.4 응답 필드 매핑

응답은 **해당 시·도에 속한 측정소 목록**으로 내려온다. (예: 서울 → 약 25개 구별 측정소)

| 필드 | 의미 | 서비스 내 사용처 |
|---|---|---|
| `sidoName` | 시도명 | 칩 매칭 |
| `stationName` | 측정소명 | 측정소 목록(옵션) |
| `dataTime` | 측정 기준 시각 | "○시 기준" 표기 |
| `pm10Value` | 미세먼지 농도 (㎍/㎥) | 메인 수치 |
| `pm10Grade` | 미세먼지 등급 (1~4) | 등급 판정 |
| `pm25Value` | 초미세먼지 농도 (㎍/㎥) | 메인 수치 |
| `pm25Grade` | 초미세먼지 등급 (1~4) | 등급 판정 |
| `khaiValue` | 통합대기환경지수 (CAI) | 부가 정보 |
| `khaiGrade` | 통합대기환경지수 등급 | **배경색 결정 기준** |
| `o3Value` | 오존 (ppm) | 부가 항목 |
| `no2Value` | 이산화질소 (ppm) | 부가 항목 |
| `coValue` | 일산화탄소 (ppm) | 부가 항목 |
| `so2Value` | 아황산가스 (ppm) | 부가 항목 |

**응답 구조 (JSON)**

```json
{
  "response": {
    "header": { "resultCode": "00", "resultMsg": "NORMAL_CODE" },
    "body": {
      "totalCount": 25,
      "items": [
        {
          "sidoName": "서울",
          "stationName": "중구",
          "dataTime": "2026-08-19 14:00",
          "pm10Value": "78",  "pm10Grade": "2",
          "pm25Value": "42",  "pm25Grade": "3",
          "khaiValue": "118", "khaiGrade": "3",
          "o3Value": "0.021", "no2Value": "0.032",
          "coValue": "0.5",   "so2Value": "0.003"
        }
      ],
      "pageNo": 1,
      "numOfRows": 100
    }
  }
}
```

> 모든 수치는 **문자열(String)** 로 내려옵니다. 계산 전 반드시 숫자로 변환하고, 변환 실패값은 3.5의 유효값 규칙으로 제외하세요.

### 3.5 시·도 대표값 산출 규칙 ⚠️ 중요

API는 측정소 단위로 응답하므로, **시·도 대표값은 직접 계산해야 한다.**

| 규칙 | 내용 |
|---|---|
| 산출 방식 | 해당 시·도 내 유효 측정소들의 **산술 평균**, 소수점 반올림 |
| 유효값 정의 | 값이 `null`, 빈 문자열, `"-"`, `"통신장애"`가 아닌 숫자 |
| 유효 측정소 0개 | "일시적으로 데이터를 받을 수 없습니다" 상태로 처리 |
| 등급 산출 | 평균 농도를 **3.6 등급 기준표**에 재대입해 산출 (API의 개별 grade 평균 아님) |
| 최고/최저 | 시·도 내 최고·최저 측정소를 함께 표기 (예: "가장 나쁨: 강남구 82㎍/㎥") |
| 기준 시각 | 측정소 중 가장 최신 `dataTime` 사용 |

### 3.6 등급 기준표

**미세먼지 / 초미세먼지 (에어코리아 기준)**

| 등급 | PM10 (㎍/㎥) | PM2.5 (㎍/㎥) | 색상 | grade 코드 |
|---|---|---|---|---|
| 좋음 | 0 ~ 30 | 0 ~ 15 | 파랑 | 1 |
| 보통 | 31 ~ 80 | 16 ~ 35 | 초록 | 2 |
| 나쁨 | 81 ~ 150 | 36 ~ 75 | 노랑/주황 | 3 |
| 매우나쁨 | 151 이상 | 76 이상 | 빨강 | 4 |

**통합대기환경지수 (CAI)**

| 등급 | 지수 범위 | 설명 |
|---|---|---|
| 좋음 | 0 ~ 50 | 대기오염 관련 질환자군에서도 영향이 유발되지 않을 수준 |
| 보통 | 51 ~ 100 | 만성 노출 시 경미한 영향 가능 |
| 나쁨 | 101 ~ 250 | 환자군 및 민감군에게 유해한 영향이 유발될 수 있는 수준 |
| 매우나쁨 | 251 ~ 500 | 급성 노출 시 심각한 영향 유발 |

**기타 항목 기준 (부가 표시용)**

| 항목 | 좋음 | 보통 | 나쁨 | 매우나쁨 |
|---|---|---|---|---|
| 오존 (ppm) | 0~0.03 | ~0.09 | ~0.15 | 0.15 초과 |
| 이산화질소 (ppm) | 0~0.03 | ~0.06 | ~0.2 | 0.2 초과 |
| 일산화탄소 (ppm) | 0~2 | ~9 | ~15 | 15 초과 |
| 아황산가스 (ppm) | 0~0.02 | ~0.05 | ~0.15 | 0.15 초과 |

### 3.7 캐싱 정책

에어코리아 측정값은 **1시간 단위로 갱신**되므로 실시간 재호출은 불필요하다.

| 항목 | 정책 |
|---|---|
| 캐시 단위 | 시·도별 |
| 캐시 유효 시간 | 10분 |
| 저장 위치 | 메모리(런타임 변수) — 브라우저 스토리지 미사용 |
| 캐시 히트 시 | API 호출 없이 즉시 렌더 |
| 수동 새로고침 | 상세 카드 내 "새로고침" 버튼 제공 (캐시 무시) |
| 초기 진입 | 전체 시·도를 미리 호출하지 않음. 클릭한 시·도만 호출 |

### 3.8 서비스키 보안 및 호출 경로

**원칙**
- 서비스키는 프론트엔드 코드에 직접 노출하지 않는다
- 서버리스 함수(또는 프록시 API 라우트)를 두고 그 안에서만 에어코리아를 호출한다
- 환경변수(`AIRKOREA_SERVICE_KEY`)로 관리하고, 저장소에 커밋하지 않는다

**호출 경로**

```
브라우저
   │  GET /api/air?sido=서울          ← 서비스키 없음
   ▼
자체 API 라우트 (서버)
   │  GET https://apis.data.go.kr/B552584/ArpltnInforInqireSvc
   │      /getCtprvnRltmMesureDnsty?serviceKey=…&sidoName=서울&ver=1.3
   ▼
에어코리아
```

| 계층 | 경로 | 노출 정보 |
|---|---|---|
| 클라이언트 → 자체 서버 | `/api/air?sido={시도명}` | 시도명만 |
| 자체 서버 → 에어코리아 | 3.1 Base URL + 3.3 파라미터 | 서비스키 포함 |

- 자체 API 라우트는 **3.5 대표값 산출까지 끝낸 결과**만 클라이언트에 내려준다 (측정소 원본 배열을 그대로 전달하지 않는다)
- CORS 이슈도 이 구조에서 자연히 해결된다 (에어코리아는 브라우저 직접 호출 시 CORS 차단)

---

## 4. 정보 구조 (IA)

```
[단일 페이지]
│
├─ 헤더
│   └─ 서비스명 / 기준 시각 / 새로고침
│
├─ 칩 영역  ← 항상 상단 고정
│   └─ 17개 시·도 칩 (선택 상태 강조)
│
├─ 상세 카드 영역
│   ├─ 지역명 + 종합 등급 배지
│   ├─ 메인 수치 (PM10 / PM2.5)
│   ├─ 행동 요령 문구
│   ├─ 통합대기환경지수(CAI)
│   └─ 기타 항목 (오존/이산화질소/일산화탄소/아황산가스) — 접기 가능
│
└─ 푸터
    └─ 데이터 출처 · 갱신 주기 안내
```

**칩 노출 순서** (지역 인접성 기준, 사용자 인지 부담 최소화)

```
서울  인천  경기  강원
대전  세종  충북  충남
광주  전북  전남
대구  부산  울산  경북  경남
제주
```

---

## 5. 화면 정의

### 5.1 초기 화면 (지역 미선택)

| 요소 | 내용 |
|---|---|
| 배경 | 중립 색상 (그라디언트 없음, 무채색 계열) |
| 헤더 | 서비스명 + 한 줄 설명 "우리 지역 공기, 한 번의 클릭으로" |
| 칩 | 17개 전부 비선택 상태 |
| 본문 | 안내 문구 — "지역을 선택해 주세요" + 일러스트/아이콘 |

### 5.2 로딩 상태

| 요소 | 내용 |
|---|---|
| 트리거 | 칩 클릭 직후 ~ 응답 도착 |
| 칩 | 선택된 칩은 즉시 선택 상태로 전환 (응답 대기 X) |
| 카드 | 스켈레톤 UI (수치 자리를 회색 블록으로 표시) |
| 배경 | 이전 색 유지, 응답 도착 시 새 색으로 전환 |
| 최소 노출 | 200ms — 캐시 히트 시 깜빡임 방지 |
| 타임아웃 | 8초 초과 시 에러 상태로 전환 |

### 5.3 결과 화면 (핵심)

**배경 — 등급색 전체 전환**

| 등급 | 배경 톤 | 텍스트 |
|---|---|---|
| 좋음 | 맑은 파랑 계열 그라디언트 | 흰색 |
| 보통 | 초록 계열 그라디언트 | 흰색 |
| 나쁨 | 주황 계열 그라디언트 | 짙은 남색 또는 흰색 (대비 확보) |
| 매우나쁨 | 짙은 빨강 계열 그라디언트 | 흰색 |
| 데이터 없음 | 무채색 | 진회색 |

- **전환 애니메이션**: 0.6초 이내 부드러운 색상 트랜지션
- **색 결정 기준**: PM2.5 등급과 PM10 등급 중 **더 나쁜 쪽**을 채택 (사용자 안전 우선)
- **접근성**: 색만으로 정보를 전달하지 않는다 — 등급 텍스트 배지를 반드시 병기, 명도 대비 4.5:1 이상 확보

> ⚠️ **등급색은 단독으로 쓸 수 없습니다 (검증 결과)**
>
> 등급 4색을 색각 이상 시뮬레이션에 넣어보니 **`보통`(초록)과 `매우나쁨`(빨강)이 적록색약에서 ΔE 4.1**로, 사실상 구분되지 않습니다 (기준 8 이상). 나쁨을 노랑으로 바꿔도 동일하게 실패합니다 — 빨강↔초록은 신호등 관례상 바꿀 수 없는 조합이라 **색 자체로는 해결되지 않는 문제**입니다.
>
> 하필 이 두 등급이 "나가도 되나"의 판단이 정반대로 갈리는 지점입니다. 따라서 등급을 표시하는 모든 자리(배경, 배지, 칩, 툴팁, 그래프)에 **아이콘 + 텍스트 라벨을 반드시 함께** 넣습니다. 남성 약 8%가 적록색약이므로 예외 상황이 아닙니다.
>
> | 등급 | 아이콘 | 텍스트 |
> |---|---|---|
> | 좋음 | ☀️ / 웃는 얼굴 | `좋음` |
> | 보통 | 🙂 / 평범한 얼굴 | `보통` |
> | 나쁨 | 😷 / 마스크 | `나쁨` |
> | 매우나쁨 | ⚠️ / 경고 | `매우나쁨` |

**카드 구성 요소**

| 순번 | 요소 | 표시 예시 |
|---|---|---|
| 1 | 지역명 | `서울특별시` |
| 2 | 종합 등급 배지 | `나쁨` (큰 텍스트 + 아이콘) |
| 3 | 기준 시각 | `2026-08-19 14:00 기준` |
| 4 | 초미세먼지 (PM2.5) | `42 ㎍/㎥ · 나쁨` ← 가장 크게 |
| 5 | 미세먼지 (PM10) | `78 ㎍/㎥ · 보통` |
| 6 | 행동 요령 | 등급별 문구 (5.4 참조) |
| 7 | 통합대기환경지수 | `CAI 118 · 나쁨` + 게이지 바 |
| 8 | 지역 내 편차 | `가장 나쁨: 강남구 92 / 가장 좋음: 은평구 31` |
| 9 | **24시간 추이 그래프** | Chart.js 선 그래프 — 중립 표면 카드 안에 배치 (5.7 참조) |
| 10 | 기타 항목 (접기) | 오존 / 이산화질소 / 일산화탄소 / 아황산가스 |
| 11 | 측정소 수 | `측정소 24곳 평균` |

### 5.4 등급별 행동 요령 문구

| 등급 | 메인 문구 | 세부 안내 |
|---|---|---|
| 좋음 | 마음껏 나가도 좋은 날이에요 | 야외 활동과 환기 모두 자유롭게 하셔도 좋습니다. |
| 보통 | 평소처럼 활동해도 괜찮아요 | 민감군은 장시간 무리한 실외 활동을 조금 줄여 주세요. |
| 나쁨 | 외출할 땐 마스크를 챙기세요 | 장시간 실외 활동을 줄이고, 창문은 되도록 닫아 두세요. 민감군은 실외 활동을 자제해 주세요. |
| 매우나쁨 | 오늘은 실내에 머무는 걸 권해요 | 실외 활동을 삼가고, 부득이 외출 시 KF80 이상 보건용 마스크를 착용하세요. 환기 대신 공기청정기를 사용하세요. |
| 데이터 없음 | 지금은 정보를 가져올 수 없어요 | 측정소 점검 중일 수 있습니다. 잠시 후 다시 시도해 주세요. |

> 민감군 정의: 어린이, 노인, 호흡기·심혈관 질환자, 임산부

### 5.5 에러 화면

| 상황 | 화면 처리 | 사용자 액션 |
|---|---|---|
| 네트워크 오류 | 무채색 배경 + "연결이 원활하지 않아요" | [다시 시도] 버튼 |
| API 응답 오류 (500 등) | "데이터를 불러오지 못했어요" | [다시 시도] 버튼 |
| 인증키 오류 | 사용자에겐 일반 오류로 표시, 콘솔에만 상세 기록 | [다시 시도] |
| 트래픽 한도 초과 | "오늘 조회 가능한 횟수를 모두 사용했어요" | 안내만 |
| 유효 측정소 0개 | "이 지역은 지금 측정값이 없어요" + 다른 지역 유도 | 칩 재선택 유도 |
| 타임아웃 (8초) | "응답이 늦어지고 있어요" | [다시 시도] |

**공통 원칙**: 에러 상황에서도 **칩 영역은 항상 살아 있어야 한다.** 다른 지역은 계속 조회 가능해야 한다.

### 5.6 반응형

| 브레이크포인트 | 칩 배치 | 카드 |
|---|---|---|
| 모바일 (~767px) | 가로 스크롤 없이 여러 줄 wrap, 한 손 터치 크기 (최소 44×44px) | 1열 세로 스택 |
| 태블릿 (768~1023px) | 4~5개씩 wrap | 2열 그리드 |
| 데스크톱 (1024px~) | 지역 그룹별 줄바꿈 | 좌: 메인 수치 / 우: 부가 항목 2단 |

### 5.7 24시간 추이 그래프 (F-17)

**라이브러리: Chart.js v4** (React 사용 시 `react-chartjs-2` 래퍼)

| 항목 | 결정 |
|---|---|
| 차트 종류 | 선 그래프 (line) |
| 시리즈 | 2개 — 초미세먼지(PM2.5), 미세먼지(PM10) |
| y축 | **단일 축, ㎍/㎥** — 두 값이 같은 단위·같은 측정량이므로 하나의 축이 맞습니다 |
| x축 | 최근 24시간, 1시간 간격 24포인트 |
| 데이터 출처 | 시·도별 **대표 측정소 1곳** (`getMsrstnAcctoRltmMesureDnsty`, `dataTerm=DAILY`) |
| 캐시 | 30분 (원본이 1시간 주기 갱신) |

> 🚫 **이중 y축 금지.** PM2.5를 오른쪽 축에 따로 그리면 두 선의 교차점이 아무 의미가 없는데도 의미 있어 보입니다. 같은 축에 그리면 PM2.5가 항상 PM10보다 아래에 오는 것이 곧 정보입니다.

**⚠️ 카드 수치와 그래프 수치가 다릅니다**

5.3 카드는 시·도 내 전 측정소 **평균**, 그래프는 **대표 측정소 1곳**입니다. 이 불일치를 사용자가 오류로 오해하지 않도록:

- 그래프 제목 아래에 `중구 측정소 기준` 을 **항상** 표기
- 그래프 영역에 시·도 평균 수치를 함께 표기하지 않는다
- (운영계정 승인 후) 전 측정소 시간대별 평균으로 전환 가능 — 호출량 17배

**색상**

차트 시리즈 색은 등급색이 아니라 **식별용 카테고리 색**입니다. 등급색을 선에 쓰면 시간마다 색이 바뀌어 "어느 선이 PM2.5인지"를 알 수 없게 됩니다.

| 역할 | 라이트 | 다크 |
|---|---|---|
| PM2.5 (시리즈 1) | `#2a78d6` (파랑) | `#3987e5` |
| PM10 (시리즈 2) | `#eb6834` (주황) | `#d95926` |
| 차트 표면 | `#fcfcfb` | `#1a1a19` |
| 그리드 (헤어라인 1px) | `#e1e0d9` | `#2c2c2a` |
| 축·라벨 텍스트 | `#898781` | `#898781` |

> 이 조합은 색각 이상 시뮬레이션 검증을 통과했습니다 — 최악 쌍 CVD ΔE 24.7(라이트) / 26.8(다크), 일반 시야 ΔE 33.6 / 31.8. (기준: CVD ≥ 8, 일반 ≥ 15)

**🔴 차트는 등급색 배경 위에 직접 올리지 않습니다**

배경이 등급에 따라 파랑↔빨강으로 바뀌므로, 그 위에 차트를 그리면 위 색상 검증이 전부 무효가 됩니다. 차트는 반드시 **중립 표면(`#fcfcfb`)의 카드 안**에 넣고, 그 카드를 등급색 배경 위에 올리세요.

**마크 사양**

| 요소 | 값 |
|---|---|
| 선 굵기 | 2px, 라운드 조인/캡 |
| 보간 | `tension: 0` (직선) — 곡선 보간은 없는 값을 있는 것처럼 만듭니다 |
| 포인트 | 평시 `pointRadius: 0`, 호버 시 `pointHoverRadius: 5` |
| 히트 영역 | `pointHitRadius: 24` — 8px 점은 손가락으로 못 맞춥니다 |
| 그리드 | y축 가로선만, 1px 실선(점선 금지). x축 세로 그리드 없음 |
| y축 | `beginAtZero: true`, 축 테두리 제거 |
| x축 눈금 | 3시간 간격만 표시 (24개 전부 표시 시 겹침) |

**범례·라벨**

- 시리즈가 2개이므로 **범례는 항상 표시**합니다 (색만으로 구분하게 두지 않음)
- 범례 키는 박스가 아닌 **선 모양** — `usePointStyle: true`, `pointStyle: 'line'`
- **모든 점에 숫자를 붙이지 않습니다.** 끝점(현재값) 하나만 직접 라벨
- 축·범례·값 텍스트는 시리즈 색을 입지 않습니다. 회색 텍스트 토큰 + 옆에 색 키

**툴팁 (기본 제공)**

| 규칙 | 내용 |
|---|---|
| 모드 | `interaction: { mode: 'index', intersect: false }` — 선을 정확히 짚지 않아도 값이 뜸 |
| 내용 | 해당 시각의 **두 시리즈 값을 한 번에** + 각각의 등급 텍스트 |
| 위계 | 값이 크고 진하게, 시리즈명은 보조 |
| 크로스헤어 | 세로 헤어라인은 Chart.js 기본 기능이 아니므로 `afterDatasetsDraw` 커스텀 플러그인으로 구현 |
| 키보드 | 포커스 시에도 호버와 동일한 정보 제공 |
| 보안 | 커스텀 HTML 툴팁 사용 시 `textContent`로 삽입 (`innerHTML` 금지) |

**접근성 — 표 뷰 필수**

툴팁은 정보를 **보조**할 뿐 잠그지 않습니다. 그래프 아래에 `<details>`로 접힌 24행 표(시각 / PM2.5 / PM10 / 등급)를 제공해, 마우스 없이도 모든 값에 도달할 수 있어야 합니다.

**기타 구현 주의**

- **SSR 회피**: Chart.js는 canvas가 필요하므로 Next.js에서 `dynamic(() => import('...'), { ssr: false })`
- **번들 크기**: `chart.js/auto` 대신 필요한 컨트롤러·스케일만 `Chart.register()` (line/point/linear/category/tooltip/legend)
- **반응형**: `responsive: true, maintainAspectRatio: false` + 래퍼 div에 고정 높이
- **모션 축소**: `prefers-reduced-motion` 시 `animation: false`
- **데이터 순서**: API 응답은 최신순이므로 그리기 전 `reverse()`
- **결측 처리**: `"-"`, `null`은 `null`로 변환해 선을 끊는다 (0으로 채우지 않는다 — 0㎍/㎥는 "좋음"이라는 거짓말입니다)

---

## 6. 기능 요구사항

### 6.1 우선순위 정의
- **P0** — 없으면 서비스가 성립하지 않음 (MVP 필수)
- **P1** — 있어야 완성도가 확보됨
- **P2** — v2 이후 검토

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F-01 | 시·도 칩 렌더 | 17개 시·도를 칩으로 표시 | P0 |
| F-02 | 칩 클릭 조회 | 클릭 시 해당 시·도 대기오염정보 API 호출 | P0 |
| F-03 | 시·도 대표값 산출 | 측정소 평균 계산 및 등급 판정 (3.5 규칙) | P0 |
| F-04 | 결과 카드 렌더 | PM10/PM2.5 수치 + 등급 표시 | P0 |
| F-05 | 배경색 등급 전환 | 등급에 따른 전체 배경 색상 변경 | P0 |
| F-06 | 행동 요령 표시 | 등급별 문구 노출 | P0 |
| F-07 | 로딩 상태 | 스켈레톤 UI | P0 |
| F-08 | 에러 상태 | 상황별 에러 화면 + 재시도 | P0 |
| F-09 | 통합대기환경지수 표시 | CAI 값 + 등급 + 게이지 | P1 |
| F-10 | 기타 항목 표시 | 오존/NO2/CO/SO2 — 접기·펼치기 | P1 |
| F-11 | 캐싱 | 시·도별 10분 캐시 | P1 |
| F-12 | 수동 새로고침 | 캐시 무시 재조회 | P1 |
| F-13 | 지역 내 편차 표시 | 최고·최저 측정소 표기 | P1 |
| F-14 | 반응형 레이아웃 | 모바일/태블릿/데스크톱 | P1 |
| F-15 | 접근성 | 키보드 탐색, 스크린리더, 색 대비 | P1 |
| F-16 | URL 공유 | `?sido=서울` 형태로 지역 상태 URL 반영 | P2 |
| F-17 | 24시간 추이 그래프 | Chart.js 선 그래프, PM2.5·PM10 2시리즈 (5.7 사양) | **P1** |
| F-17a | 그래프 표 뷰 | 접기식 24행 표 — 툴팁 없이도 값 도달 가능 | **P1** |
| F-18 | 내일 예보 | 대기오염 예보 API 연동 | P2 |
| F-19 | 시·군·구 2단계 | 시도 → 하위 지역 드릴다운 | P2 |
| F-20 | 전국 한눈에 보기 | 17개 시도 등급을 그리드로 동시 표시 | P2 |

### 6.2 상세 요구사항

**F-01 시·도 칩 렌더**
- 17개 고정 목록을 코드 내 상수로 관리 (API 호출로 목록을 가져오지 않는다)
- 각 칩은 `sidoName` API 파라미터 값과 정확히 일치해야 함 (예: `서울` — `서울특별시` 아님)
- 화면 표시명은 별도 (`서울`), API 값은 내부 매핑
- 선택된 칩은 시각적으로 명확히 구분 (테두리 + 배경 반전)

**F-02 칩 클릭 조회**
- 이미 선택된 칩 재클릭 시: 재조회하지 않음 (새로고침 버튼으로만)
- 응답 대기 중 다른 칩 클릭 시: 이전 요청 취소, 최신 클릭 기준으로 처리
- 클릭 이벤트는 즉시 UI에 반영 (선택 상태 → 로딩 → 결과)

**F-05 배경색 등급 전환**
- PM2.5 등급과 PM10 등급 중 나쁜 쪽을 채택
- 두 값 모두 없으면 CAI 등급으로 대체, 그것도 없으면 무채색
- CSS transition으로 처리, 애니메이션 축소 설정(`prefers-reduced-motion`) 존중

**F-15 접근성**
- 칩은 `role="tab"` 또는 버튼 시맨틱, 키보드 Tab/Enter로 조작 가능
- 결과 영역은 `aria-live="polite"`로 변경 사항 안내
- 등급은 색 + 텍스트 배지 병기 (색맹 사용자 고려)
- 모든 텍스트 명도 대비 4.5:1 이상

---

## 7. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 성능 | 초기 로드 2초 이내 (LCP), 칩 클릭 → 렌더 1초 이내 |
| 가용성 | API 장애 시에도 페이지 자체는 정상 동작 (칩 조작 가능) |
| 보안 | 서비스키 서버 측 보관, 클라이언트 노출 금지 |
| 저장소 | 브라우저 로컬 스토리지 미사용 (메모리 캐시만) |
| 브라우저 | Chrome / Safari / Edge 최신 2개 버전, iOS Safari, Android Chrome |
| 데이터 신뢰성 | 화면에 항상 `dataTime` 기준 시각 명시. 30분 이상 지난 데이터는 "지연" 뱃지 표시 |
| 법적 표기 | 푸터에 "출처: 한국환경공단 에어코리아" 명시 |

---

## 8. 기술 스택 제안

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | API 라우트로 서비스키를 숨길 수 있어 별도 서버 불필요 |
| 언어 | TypeScript | 응답 필드가 많아 타입 정의로 실수 방지 |
| 스타일 | Tailwind CSS | 등급별 색상 토큰 관리와 반응형 구현이 빠름 |
| 배포 | Vercel | 무료, 환경변수 관리 내장, GitHub 연동 자동 배포 |
| 상태 관리 | React useState + 커스텀 훅 | 단일 페이지 규모라 별도 라이브러리 불필요 |
| 캐시 | 모듈 스코프 Map 객체 | 10분 TTL 자체 구현으로 충분 |
| **차트** | **Chart.js v4** (+ `react-chartjs-2`) | canvas 렌더라 24포인트 2시리즈에서 가볍고, 툴팁·범례·반응형이 옵션만으로 해결됨 |

**차트 라이브러리 관련 메모**

- Chart.js는 canvas 기반이라 **SSR이 불가**합니다. Next.js에서는 차트 컴포넌트를 `dynamic(..., { ssr: false })`로 감싸세요. (SVG 기반 라이브러리라면 불필요한 단계입니다.)
- 세로 크로스헤어는 Chart.js 내장 기능이 아니라 **커스텀 플러그인**으로 직접 구현합니다 (5.7 참조). 20줄 내외입니다.
- `import Chart from 'chart.js/auto'` 는 전체 차트 종류를 번들에 넣습니다. 선 그래프 하나만 쓰므로 필요한 모듈만 `Chart.register()` 하세요.

> 이 스택은 개발 편의를 우선한 제안입니다. 프로토타입 단계에서는 단일 HTML 파일로 먼저 만들고, 서비스키만 프록시 처리하는 방식도 가능합니다.

---

## 9. 개발 단계

| 단계 | 작업 | 산출물 |
|---|---|---|
| 0단계 | 공공데이터포털 회원가입 → 대기오염정보 API 활용 신청 → 서비스키 발급 | 서비스키 |
| 1단계 | 프로젝트 셋업, 17개 시도 상수, API 프록시 라우트 | 호출 성공 확인 |
| 2단계 | 시·도 대표값 산출 로직 + 등급 판정 함수 | 유틸 함수 + 테스트 |
| 3단계 | 칩 UI + 선택 상태 | 정적 화면 |
| 4단계 | 결과 카드 + 배경색 전환 (F-04, F-05, F-06) | 핵심 화면 완성 |
| 5단계 | 로딩·에러 상태 (F-07, F-08) | 상태 처리 완료 |
| 6단계 | 부가 항목·캐싱·편차 표시 (F-09~F-13) | P1 기능 완료 |
| 6.5단계 | **Chart.js 추이 그래프 + 표 뷰 (F-17, F-17a)** — 대표 측정소 상수 정의 → 측정소별 API 연동 → 차트 구현 → 크로스헤어 플러그인 | 그래프 완성 |
| 7단계 | 반응형 + 접근성 (F-14, F-15) | 마감 |
| 8단계 | 배포 및 검증 | 라이브 URL |

---

## 10. 검증 체크리스트

**데이터 정확성**
- [ ] 17개 시·도 모두 정상 응답하는가
- [ ] 에어코리아 공식 사이트 수치와 대조했을 때 오차 범위 내인가
- [ ] `pm25Value`가 없고 `pm10Value`만 있는 측정소가 섞여도 평균이 정확한가
- [ ] `"-"`, `null`, `"통신장애"` 값이 평균 계산에 포함되지 않는가
- [ ] 등급 경계값(PM10 = 30, 31, 80, 81 / PM2.5 = 15, 16, 35, 36)에서 등급이 정확히 갈리는가

**UI/UX**
- [ ] 네 등급 배경색에서 모든 텍스트가 읽히는가 (대비 4.5:1)
- [ ] 칩 → 칩 연속 클릭 시 화면이 깨지지 않는가
- [ ] 응답 지연 중 다른 칩을 눌렀을 때 잘못된 결과가 표시되지 않는가
- [ ] 모바일에서 칩 터치 영역이 44px 이상인가
- [ ] 키보드만으로 모든 칩을 조작할 수 있는가

**차트 (F-17)**
- [ ] y축이 **하나**인가 (이중 축이 아닌가)
- [ ] 그래프가 등급색 배경이 아닌 중립 표면 카드 안에 있는가
- [ ] `중구 측정소 기준` 같은 출처 표기가 항상 보이는가
- [ ] 결측 시간대에서 선이 끊기는가 (0으로 이어지지 않는가)
- [ ] API 응답을 뒤집어서 시간 순서가 왼쪽→오른쪽으로 맞는가
- [ ] 선을 정확히 짚지 않아도 툴팁이 뜨는가 (`mode: 'index'`)
- [ ] 모바일에서 손가락으로 포인트를 맞출 수 있는가 (`pointHitRadius`)
- [ ] 표 뷰만으로 24개 값 전부에 도달할 수 있는가
- [ ] 흑백 출력 / 색각 이상 시뮬레이터에서 두 선을 구분할 수 있는가
- [ ] 축·범례 텍스트가 시리즈 색이 아닌 회색 계열인가
- [ ] x축 눈금이 겹치지 않는가 (3시간 간격)
- [ ] 등급 표시 자리마다 아이콘 + 텍스트가 함께 있는가

**안정성**
- [ ] 네트워크를 끊은 상태에서 에러 화면이 정상 표시되는가
- [ ] 잘못된 서비스키로 호출 시 사용자에게 키 정보가 노출되지 않는가
- [ ] 브라우저 개발자 도구 Network 탭에서 서비스키가 보이지 않는가
- [ ] 새벽 시간대(측정값 미갱신)에도 화면이 정상인가

---

## 11. 열린 이슈 / 다음 결정 사항

| # | 이슈 | 선택지 | 상태 |
|---|---|---|---|
| 1 | 서비스명 확정 | "오늘의 공기" / "우리동네 공기" / 기타 | 미정 |
| 2 | 세종시 측정소 수가 적어 평균 신뢰도 낮음 | 그대로 표기 / 측정소 수 경고 병기 | 경고 병기 권장 |
| 3 | 배경 그라디언트 vs 단색 | 프로토타입에서 비교 후 결정 | 미정 |
| 4 | 초기 진입 시 기본 선택 지역 | 없음(안내 화면) / 서울 자동 선택 | 안내 화면 권장 |
| 5 | 다크 모드 지원 여부 | 등급색이 이미 배경을 지배하므로 불필요할 수 있음 | 미정 |
| 6 | **시·도별 대표 측정소 17곳 선정 기준** | 도청·시청 소재지 / 인구 최다 지역 / 도심 대표성 | **미정 — 6.5단계 착수 전 필수** |
| 7 | 그래프 값과 카드 평균값 불일치 | 대표 측정소 명시(현재안) / 운영계정 승인 후 전 측정소 평균 전환 | 명시로 시작 |

---

## 부록 A. 시·도 코드 매핑

| 표시명 | API `sidoName` | 정식 명칭 |
|---|---|---|
| 서울 | 서울 | 서울특별시 |
| 인천 | 인천 | 인천광역시 |
| 경기 | 경기 | 경기도 |
| 강원 | 강원 | 강원특별자치도 |
| 대전 | 대전 | 대전광역시 |
| 세종 | 세종 | 세종특별자치시 |
| 충북 | 충북 | 충청북도 |
| 충남 | 충남 | 충청남도 |
| 광주 | 광주 | 광주광역시 |
| 전북 | 전북 | 전북특별자치도 |
| 전남 | 전남 | 전라남도 |
| 대구 | 대구 | 대구광역시 |
| 부산 | 부산 | 부산광역시 |
| 울산 | 울산 | 울산광역시 |
| 경북 | 경북 | 경상북도 |
| 경남 | 경남 | 경상남도 |
| 제주 | 제주 | 제주특별자치도 |

## 부록 B. API 엔드포인트 요약 (개발용 치트시트)

```
# Base URL
https://apis.data.go.kr/B552584/ArpltnInforInqireSvc

# P0 — 시도별 실시간 측정정보
GET {BASE}/getCtprvnRltmMesureDnsty
    ?serviceKey={KEY}&returnType=json&numOfRows=100&pageNo=1&sidoName=서울&ver=1.3

# P1 — 측정소별 실시간 측정정보 (24시간 추이 그래프용)
GET {BASE}/getMsrstnAcctoRltmMesureDnsty
    ?serviceKey={KEY}&returnType=json&numOfRows=24&pageNo=1
    &stationName=중구&dataTerm=DAILY&ver=1.3
# ※ 응답은 최신순 → 그리기 전 reverse()

# v2 — 대기질 예보통보
GET {BASE}/getMinuDustFrcstDspth
    ?serviceKey={KEY}&returnType=json&searchDate=2026-08-19&InformCode=PM10

# 별도 서비스 — 측정소 정보 (좌표/주소)
https://apis.data.go.kr/B552584/MsrstnInfoInqireSvc

# 별도 서비스 — 대기오염 통계
https://apis.data.go.kr/B552584/ArpltnStatsSvc
```

환경변수

```
AIRKOREA_BASE_URL=https://apis.data.go.kr/B552584/ArpltnInforInqireSvc
AIRKOREA_SERVICE_KEY=발급받은_Decoding_키
```

## 부록 C. 참고 링크

- 공공데이터포털 — 한국환경공단_에어코리아_대기오염정보 (ID 15073861): `https://www.data.go.kr/data/15073861/openapi.do`
- 공공데이터포털 — 에어코리아_측정소정보 (ID 15073877): `https://www.data.go.kr/data/15073877/openapi.do`
- 공공데이터포털 — 에어코리아_대기오염통계 현황 (ID 15073855): `https://www.data.go.kr/data/15073855/openapi.do`
- 에어코리아 통합대기환경지수 설명: `https://www.airkorea.or.kr/web/khaiInfo?pMENU_NO=129`
- 에어코리아 실시간 대기정보: `https://www.airkorea.or.kr/index`
